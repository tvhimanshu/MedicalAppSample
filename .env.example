# VectorSearchCore backend (run locally or point to deployed instance)
VECTOR_SEARCH_URL=http://localhost:4000
VECTOR_SEARCH_EMAIL=medrag@demo.com
VECTOR_SEARCH_PASSWORD=MedRag@2024!

# OpenAI — required for embeddings + RAG generation
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small

# RAG tuning
RAG_TOP_K=6
RAG_MIN_SCORE=0.30
